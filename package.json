{
  "name": "W3",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "prestart": "cd lib && npm install && cd .. && npm install",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
